import { subscriptionService as subService } from '../services/subscriptionService';

// Re-export the subscription service
export const subscriptionService = subService;

export default {
  subscriptionService
};
